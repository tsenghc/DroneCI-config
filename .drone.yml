kind: pipeline
type: docker
name: drone-test

steps:
  - name: python-lint
    image: python:3.8.7
    commands:
      - pip install flake8
      - flake8 .
    when:
      event:
        - push
        - pull_request
